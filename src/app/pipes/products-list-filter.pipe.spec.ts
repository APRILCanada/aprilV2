import { ProductsListFilterPipe } from './products-list-filter.pipe';

describe('ProductsListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
