import { NichesFilterPipe } from './niches-filter.pipe';

describe('NichesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NichesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
