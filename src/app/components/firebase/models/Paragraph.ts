import { FormGroup } from "@angular/forms";

export class Paragraph {
	order: number;
	title:string;
	desc:string;


	constructor(form: FormGroup){
		// if(form != null){
		// 	// this.order = form.controls["order"];
		// }
	
	}
}
